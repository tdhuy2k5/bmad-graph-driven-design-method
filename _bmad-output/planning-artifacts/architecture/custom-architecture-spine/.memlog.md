# memlog for custom-architecture-spine
- type: init
  scope: project
  purpose: build-substrate
  altitude: initiative
- type: decision
  id: AD-1
  binds: catalog/pages
  prevents: CDN cache misses and slow TTFB
  rule: Catalog pages must be 100% SSG; SSR is strictly forbidden.
- type: decision
  id: AD-2
  binds: comments, auth
  prevents: Server coupling with BaaS and slow page generation
  rule: Dynamic behavior must be isolated to Client Components using the Firebase JS SDK.
- type: decision
  id: AD-3
  binds: checkout
  prevents: Server-side order state management and checkout backend overhead
  rule: Cart state and Zalo handoff link generation must be purely client-side.
- type: decision
  id: AD-4
  binds: product-data
  prevents: Database roundtrips and complex CMS integration
  rule: Product data must be managed statically in the codebase (products.json).
- type: decision
  id: AD-5
  binds: comments-security
  prevents: Spam and unauthorized writes from compromised clients
  rule: Firestore Security Rules are the only trusted enforcement mechanism; client UI is UX only.
- type: event
  text: spine drafted via fast path
- type: decision
  id: AD-6
  binds: file-system
  prevents: Rigid coupling of exact URLs and Page Instances
  rule: Replace structural seed with Zoning Laws. Specific URLs and Information Architecture are delegated entirely to the UX Agent.
- type: decision
  id: AD-7
  binds: documentation
  prevents: Missing visual representation of architecture
  rule: Add mandatory Mermaid System Context and Data Model diagrams.
- type: event
  text: architecture spine overwritten and finalized according to Winston System Architect core directives.
